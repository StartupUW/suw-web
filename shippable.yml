language: python

python:
  - 2.7

install:
  - pip install -r requirements.txt

env:
  - DJANGO_SETTINGS_MODULE=web.shippable_settings

before_script: 
  - psql -c 'create database test;' -U postgres
  - mkdir -p shippable/testresults
  - mkdir -p shippable/codecoverage

script:
  - python manage.py test
