language: node_js

node_js:
  - 0.11

env:
  - PG_USER=postgres
  - PG_PASSWORD=

before_install:
  - cd web/

before_script:
  - psql -c 'create database suw;' -U postgres
  - python ../scripts/test/populate_db.py

script:
  - npm test
